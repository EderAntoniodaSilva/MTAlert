//Select no banco para trazer usuário
$usuarios = array("eder","nany","tobby");

$usuarioLogin = "eder";
  
if(!in_array($usuarioLogin,$usuarios)){
        echo "Login Inválido";
  }else{
      //Chamar session página inicial
        echo "Usuário Valido";
 }

